# ============================================
# WA Finance Bot - Nginx Configuration
# ============================================
# Konfigurasi untuk production dengan domain
# Sesuaikan domain dan SSL certificate paths

# Upstream untuk Backend Laravel
upstream backend {
    server backend:9000;
}

# Upstream untuk n8n
upstream n8n {
    server n8n:5678;
}

# Upstream untuk GoWA
upstream gowa {
    server gowa:3000;
}

# Upstream untuk Frontend (production - static files)
upstream frontend {
    server frontend-prod:80;
}

# ============================================
# Frontend (catatuang.rivaldev.site)
# ============================================
server {
    listen 80;
    server_name catatuang.rivaldev.site;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name catatuang.rivaldev.site;

    ssl_certificate /etc/nginx/ssl/catatuang.rivaldev.site.crt;
    ssl_certificate_key /etc/nginx/ssl/catatuang.rivaldev.site.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    access_log /var/log/nginx/frontend-access.log;
    error_log /var/log/nginx/frontend-error.log;

    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://frontend;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}

# ============================================
# Backend API (apicatat.rivaldev.site)
# ============================================
server {
    listen 80;
    server_name apicatat.rivaldev.site;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name apicatat.rivaldev.site;

    ssl_certificate /etc/nginx/ssl/apicatat.rivaldev.site.crt;
    ssl_certificate_key /etc/nginx/ssl/apicatat.rivaldev.site.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    root /var/www/public;
    index index.php index.html;

    access_log /var/log/nginx/backend-access.log;
    error_log /var/log/nginx/backend-error.log;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass backend;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME /var/www/public$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ============================================
# n8n (n8n.rivaldev.site)
# ============================================
server {
    listen 80;
    server_name n8n.rivaldev.site;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name n8n.rivaldev.site;

    ssl_certificate /etc/nginx/ssl/n8n.rivaldev.site.crt;
    ssl_certificate_key /etc/nginx/ssl/n8n.rivaldev.site.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    access_log /var/log/nginx/n8n-access.log;
    error_log /var/log/nginx/n8n-error.log;

    location / {
        proxy_pass http://n8n;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# ============================================
# GoWA (gowa.rivaldev.site)
# ============================================
server {
    listen 80;
    server_name gowa.rivaldev.site;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name gowa.rivaldev.site;

    ssl_certificate /etc/nginx/ssl/gowa.rivaldev.site.crt;
    ssl_certificate_key /etc/nginx/ssl/gowa.rivaldev.site.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    access_log /var/log/nginx/gowa-access.log;
    error_log /var/log/nginx/gowa-error.log;

    location / {
        proxy_pass http://gowa;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

